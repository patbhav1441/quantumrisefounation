# 🎨 System Architecture Diagram - quantumrisefoundation.org

```
┌──────────────────────────────────────────────────────────────────────────────┐
│                                                                              │
│                  🌍 quantumrisefoundation.org                                │
│                                                                              │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │                         User's Browser                               │  │
│  │  https://quantumrisefoundation.org                                   │  │
│  └────────────────────────────┬─────────────────────────────────────────┘  │
│                               │                                             │
│                        ┌──────▼───────┐                                     │
│                        │   Frontend    │                                     │
│                        │ React + Vite  │                                     │
│                        └──────┬───────┘                                     │
│                               │                                             │
│                    ┌──────────┴──────────┐                                   │
│                    │                     │                                   │
│              ┌─────▼────┐         ┌──────▼──────┐                            │
│              │   Pages  │         │  Components │                            │
│              ├─────────┤         ├────────────┤                            │
│              │ Home    │         │ Navbar     │                            │
│              │ Auth    │         │ Footer     │                            │
│              │ Lessons │         │ Chat UI    │                            │
│              │ Dash    │         │ Cards      │                            │
│              │ Admin   │         │ Forms      │                            │
│              └─────────┘         └────────────┘                            │
│                                                                              │
│                   (Hosted on Vercel/Netlify)                                │
│                                                                              │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │                                                                      │  │
│  │                     HTTPS Request/Response                           │  │
│  │  ┌─────────────────────────────────────────────────────────────┐   │  │
│  │  │ POST /api/auth/login                                         │   │  │
│  │  │ GET /api/lessons/:id                                         │   │  │
│  │  │ POST /api/tutor/ask (with JWT token)                        │   │  │
│  │  │ GET /api/user/profile (with JWT token)                      │   │  │
│  │  └─────────────────────────────────────────────────────────────┘   │  │
│  │                                                                      │  │
│  └────────────────────────────┬─────────────────────────────────────────┘  │
│                               │                                             │
│                        ┌──────▼───────────┐                                 │
│                        │     Backend      │                                 │
│                        │   Express API    │                                 │
│                        │   Node.js        │                                 │
│                        └──────┬───────────┘                                 │
│                               │                                             │
│                  ┌────────────┴────────────┐                                │
│                  │                         │                                │
│         ┌────────▼────────┐      ┌─────────▼──────────┐                     │
│         │ Route Handlers  │      │ Services/Utils     │                     │
│         ├─────────────────┤      ├────────────────────┤                     │
│         │ /api/auth       │      │ aiTutor.js         │                     │
│         │ /api/lessons    │      │ database.js        │                     │
│         │ /api/tutor      │      │ authentication.js  │                     │
│         │ /api/user       │      │ validation.js      │                     │
│         │ /api/admin      │      │ error handlers     │                     │
│         └─────────────────┘      └────────────────────┘                     │
│                                                                              │
│             (Hosted on Railway.app or Heroku)                               │
│             https://api.quantumrisefoundation.org                           │
│                                                                              │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │                                                                      │  │
│  │              External Services & APIs                                │  │
│  │  ┌─────────────────────────────────────────────────────────────┐   │  │
│  │  │ OpenAI GPT-4 API                                            │   │  │
│  │  │ ├─ Ask questions                                           │   │  │
│  │  │ ├─ Generate exercises                                      │   │  │
│  │  │ ├─ Explain concepts                                        │   │  │
│  │  │ └─ Evaluate answers                                        │   │  │
│  │  └─────────────────────────────────────────────────────────────┘   │  │
│  │                                                                      │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│                               │                                             │
│                        ┌──────▼───────────┐                                 │
│                        │   PostgreSQL     │                                 │
│                        │   Database       │                                 │
│                        └──────┬───────────┘                                 │
│                               │                                             │
│              ┌────────────────┼────────────────┐                            │
│              │                │                │                            │
│     ┌────────▼────────┐ ┌─────▼──────┐ ┌──────▼───────┐                    │
│     │   Users Table   │ │ Lessons    │ │ Tutor Conv. │                    │
│     ├────────────────┤ ├────────────┤ ├──────────────┤                    │
│     │ id             │ │ id         │ │ id           │                    │
│     │ email          │ │ title      │ │ user_id      │                    │
│     │ password_hash  │ │ content    │ │ question     │                    │
│     │ role           │ │ discipline │ │ answer       │                    │
│     │ created_at     │ │ level      │ │ created_at   │                    │
│     └────────────────┘ └────────────┘ └──────────────┘                    │
│              │                │                │                            │
│     ┌────────▼────────┐ ┌─────▼──────┐ ┌──────▼───────┐                    │
│     │ Badges Table    │ │ Progress   │ │ Other Tables │                    │
│     └────────────────┘ └────────────┘ └──────────────┘                    │
│                                                                              │
│     (Railway.app or Heroku Postgres)                                        │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘
```

---

## 📊 Data Flow Diagrams

### Authentication Flow
```
User Browser              Backend                  Database
    │                       │                        │
    │──1. POST /signup─────>│                        │
    │   {email, password}   │                        │
    │                       │──2. Hash password─────>│
    │                       │   Create user          │
    │                       │<──3. User ID returned──│
    │                       │                        │
    │<──4. JWT Token───────│                        │
    │   {token, user_id}   │                        │
    │                       │                        │
    │────Store token────────────────────────────────>
    │  (Browser localStorage)
```

### Lesson Query Flow
```
User Browser              Backend                  Database
    │                       │                        │
    │──1. GET /lessons─────>│                        │
    │   {Authorization header with JWT}             │
    │                       │──2. Verify JWT────────>
    │                       │                        │
    │                       │<──3. User verified────│
    │                       │                        │
    │                       │──4. Query lessons────>
    │                       │   WHERE discipline=X   │
    │                       │<──5. Lesson data──────│
    │<──6. JSON Response───│                        │
    │   [{id, title, ...}] │                        │
    │                       │                        │
    │──Display lessons──────────────────────────────>
```

### AI Tutor Interaction Flow
```
User Browser              Backend                  Database    OpenAI API
    │                       │                        │            │
    │──1. POST /tutor/ask──>│                        │            │
    │   {question, history} │                        │            │
    │                       │──2. Verify JWT────────>            │
    │                       │<──3. User verified────│            │
    │                       │                        │            │
    │                       │──4. Save question────>            │
    │                       │<──5. Saved────────────│            │
    │                       │                        │            │
    │                       │──6. POST to OpenAI──────────────────>
    │                       │   {question, context}              │
    │                       │<──7. AI Response──────────────────│
    │                       │                        │            │
    │                       │──8. Save answer──────>            │
    │                       │<──9. Saved────────────│            │
    │                       │                        │            │
    │<──10. JSON Response──│                        │            │
    │    {answer, tokens}  │                        │            │
    │                       │                        │            │
    │──Display response─────────────────────────────────────────>
```

---

## 🔐 Security Architecture

```
┌──────────────────────────────────────────────────────────────┐
│                  Security Layers                             │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  Layer 1: HTTPS/TLS Encryption                              │
│  ├─ All traffic encrypted in transit                        │
│  ├─ Certificate: Let's Encrypt (automatic)                  │
│  └─ Port: 443 (HTTPS enforced)                              │
│                                                              │
│  Layer 2: CORS (Cross-Origin Resource Sharing)              │
│  ├─ Only quantumrisefoundation.org allowed                 │
│  ├─ Prevents unauthorized API access                        │
│  └─ Configured in backend/server.js                         │
│                                                              │
│  Layer 3: JWT Authentication                                │
│  ├─ Token required in Authorization header                  │
│  ├─ Token verified using JWT_SECRET                         │
│  ├─ User ID extracted from token                            │
│  └─ Scope: Valid for 24 hours (configurable)               │
│                                                              │
│  Layer 4: Password Security                                 │
│  ├─ Hashed with bcryptjs (salt rounds: 10)                 │
│  ├─ Never stored as plaintext                               │
│  ├─ Compared securely on login                              │
│  └─ Impossible to reverse                                   │
│                                                              │
│  Layer 5: Database Security                                 │
│  ├─ Connection: TLS encryption                              │
│  ├─ Credentials: Environment variables (not in code)        │
│  ├─ Access: Limited to backend only                         │
│  └─ Backups: Automatic and encrypted                        │
│                                                              │
│  Layer 6: API Key Security                                  │
│  ├─ OpenAI key in .env (never exposed)                      │
│  ├─ No key visible in frontend                              │
│  ├─ All API calls through backend                           │
│  └─ Rate limiting prevents abuse                            │
│                                                              │
│  Layer 7: Input Validation                                  │
│  ├─ All inputs validated server-side                        │
│  ├─ SQL injection prevented (parameterized queries)         │
│  ├─ XSS prevented (input sanitization)                      │
│  └─ Type checking (express-validator)                       │
│                                                              │
│  Layer 8: Error Handling                                    │
│  ├─ Generic error messages (no info leakage)                │
│  ├─ Detailed logs server-side only                          │
│  ├─ Stack traces hidden from users                          │
│  └─ Graceful degradation                                    │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

---

## 🚀 Deployment Architecture

```
┌────────────────────────────────────────────────────────────────┐
│                   quantumrisefoundation.org                     │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  Frontend Deployment (Vercel/Netlify)                         │
│  ┌──────────────────────────────────────┐                     │
│  │ git push → GitHub → Auto-trigger   │                     │
│  │ ├─ npm install                     │                     │
│  │ ├─ npm run build (Vite)            │                     │
│  │ ├─ Output: /dist folder            │                     │
│  │ └─ Deploy to CDN                   │                     │
│  │                                     │                     │
│  │ Result:                             │                     │
│  │ - Automatic SSL/TLS                │                     │
│  │ - Global CDN                       │                     │
│  │ - ~2-3 min deploy time             │                     │
│  └──────────────────────────────────────┘                     │
│                 ↓                                               │
│         quantumrisefoundation.org                              │
│                 ↓                                               │
│  Backend Deployment (Railway/Heroku)                           │
│  ┌──────────────────────────────────────┐                     │
│  │ git push → GitHub → Auto-trigger   │                     │
│  │ ├─ npm install                     │                     │
│  │ ├─ Run migrations                  │                     │
│  │ ├─ Start server (npm start)        │                     │
│  │ └─ Deploy to container             │                     │
│  │                                     │                     │
│  │ Result:                             │                     │
│  │ - Automatic SSL/TLS                │                     │
│  │ - Environment variables injected    │                     │
│  │ - ~2-3 min deploy time             │                     │
│  └──────────────────────────────────────┘                     │
│                 ↓                                               │
│         api.quantumrisefoundation.org                          │
│                 ↓                                               │
│  Database Deployment (Railway/Heroku)                          │
│  ┌──────────────────────────────────────┐                     │
│  │ PostgreSQL Instance                │                     │
│  │ ├─ Automatic backups               │                     │
│  │ ├─ TLS encryption                  │                     │
│  │ ├─ Connection pooling              │                     │
│  │ └─ Monitoring included             │                     │
│  │                                     │                     │
│  │ Result:                             │                     │
│  │ - Zero downtime                    │                     │
│  │ - Data persistence                 │                     │
│  │ - Automatic scaling                │                     │
│  └──────────────────────────────────────┘                     │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

## 📊 Component Interaction Matrix

```
┌──────────────────┬───────────────┬──────────────┬──────────────┐
│ Component        │ Depends On    │ Provides     │ Tech Stack   │
├──────────────────┼───────────────┼──────────────┼──────────────┤
│ Frontend         │ Backend API   │ UI/UX        │ React 18     │
│                  │               │              │ Vite         │
│                  │               │              │ Tailwind CSS │
├──────────────────┼───────────────┼──────────────┼──────────────┤
│ Backend API      │ Database      │ REST Endpoints│Express.js   │
│                  │ OpenAI API    │ JSON Response │ Node.js      │
├──────────────────┼───────────────┼──────────────┼──────────────┤
│ Database         │ (none)        │ Data Storage │PostgreSQL 14+│
│                  │               │ Persistence  │              │
├──────────────────┼───────────────┼──────────────┼──────────────┤
│ OpenAI           │ (cloud)       │ AI Responses │GPT-4 Turbo   │
│                  │               │ Generated    │              │
├──────────────────┼───────────────┼──────────────┼──────────────┤
│ Vercel/Netlify   │ Frontend code │ Hosting      │CDN + Caching │
│                  │ GitHub repo   │ SSL/TLS      │              │
├──────────────────┼───────────────┼──────────────┼──────────────┤
│ Railway/Heroku   │ Backend code  │ API Hosting  │Container     │
│                  │ GitHub repo   │ Auto-scaling │              │
└──────────────────┴───────────────┴──────────────┴──────────────┘
```

---

## 🔄 Request/Response Cycle

```
User Request
    │
    ▼
┌─────────────────────────────┐
│ Frontend (React Component)  │
│ └─ User clicks button      │
│ └─ onClick handler fired    │
└────────────┬────────────────┘
             │
             ▼
┌─────────────────────────────┐
│ HTTP Request (HTTPS)        │
│ ├─ Method: POST/GET/PUT     │
│ ├─ URL: /api/...            │
│ ├─ Headers: {Authorization} │
│ └─ Body: {JSON data}        │
└────────────┬────────────────┘
             │
             ▼ (Network)
┌─────────────────────────────┐
│ Backend (Express.js)        │
│ ├─ Route handler called     │
│ ├─ Middleware executed      │
│ │  └─ JWT verified         │
│ └─ Business logic           │
└────────────┬────────────────┘
             │
             ▼
┌─────────────────────────────┐
│ Database Query              │
│ ├─ SELECT/INSERT/UPDATE     │
│ ├─ SQL execution            │
│ └─ Data returned            │
└────────────┬────────────────┘
             │
             ▼
┌─────────────────────────────┐
│ Response Generation         │
│ ├─ Format data as JSON      │
│ ├─ Add status code          │
│ └─ Add headers              │
└────────────┬────────────────┘
             │
             ▼
┌─────────────────────────────┐
│ HTTP Response (HTTPS)       │
│ ├─ Status: 200/400/500      │
│ ├─ Headers: {content-type}  │
│ └─ Body: {JSON response}    │
└────────────┬────────────────┘
             │
             ▼ (Network)
┌─────────────────────────────┐
│ Frontend (React Component)  │
│ ├─ Response received        │
│ ├─ Parse JSON               │
│ ├─ Update state             │
│ └─ Re-render UI             │
└────────────┬────────────────┘
             │
             ▼
         User Sees Result
```

---

## 📱 Responsive Design Flow

```
┌─────────────────────────────────────────┐
│   quantumrisefoundation.org             │
├─────────────────────────────────────────┤
│                                         │
│  Desktop (1200px+)                      │
│  ├─ Sidebar navigation                 │
│  ├─ Main content area                  │
│  ├─ Right panel (AI Chat)              │
│  └─ Footer                             │
│                                         │
│  Tablet (768px - 1199px)                │
│  ├─ Collapsed sidebar                  │
│  ├─ Main content area                  │
│  ├─ AI Chat below (stacked)            │
│  └─ Footer                             │
│                                         │
│  Mobile (<768px)                        │
│  ├─ Hamburger menu                     │
│  ├─ Full-width content                 │
│  ├─ AI Chat drawer                     │
│  └─ Footer                             │
│                                         │
└─────────────────────────────────────────┘
```

---

## 🎯 Performance Metrics

```
Target Metrics After Deployment
│
├─ Frontend Load Time: < 3 seconds
│  ├─ Time to First Byte (TTFB): < 500ms
│  ├─ First Contentful Paint (FCP): < 1.5s
│  └─ Largest Contentful Paint (LCP): < 2.5s
│
├─ Backend Response Time: < 500ms
│  ├─ Database query: < 100ms
│  ├─ API processing: < 200ms
│  └─ Network: < 200ms
│
├─ AI Tutor Response: 2-10 seconds
│  ├─ Question received: 0ms
│  ├─ OpenAI API call: 1-5s
│  ├─ Database save: < 100ms
│  └─ Response returned: < 200ms
│
└─ Overall User Experience
   ├─ 99.9% uptime
   ├─ Mobile responsive
   ├─ Accessible (WCAG AA)
   └─ SEO optimized
```

---

*Architecture documented: November 15, 2025*
